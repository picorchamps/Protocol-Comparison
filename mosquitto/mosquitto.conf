# Mosquitto configuration for pico-launcher benchmarks.
listener 1883
allow_anonymous true

max_packet_size 268435455

# Raise per-client message queue limits so the broker does not drop
# messages when the subscriber is briefly slower than the publisher.
max_queued_messages 0
max_queued_bytes 0
